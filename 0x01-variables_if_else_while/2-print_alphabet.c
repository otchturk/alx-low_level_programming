#include <stdio.h>
#include <stdlib.h>

/**
 * main - main block
 * Description: Get a random it with 5
 * Return: 0
 */

int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
		c++;
	}
	putchar('\n');
	return (0);
}
